{
  "name": "tombsubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ maxim280596/tombsubgraph",
    "create-local": "graph create --node http://localhost:8020/ maxim280596/tombsubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ maxim280596/tombsubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 maxim280596/tombsubgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
